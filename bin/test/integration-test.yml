spring:
  datasource:
    url: none
    username: none
    password: none

  sql:
    init:
      mode: never
      platform: postgresql

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop